using Microsoft.AspNetCore.Identity;

namespace ShortenerUrlApp.WebApi.Entities
{
    // Custom Identity user; extend with profile fields in future phases if needed.
    public class ApplicationUser : IdentityUser
    {
    }
}
